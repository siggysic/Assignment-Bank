GET     /                           controllers.BankController.menu

GET     /bank/menu                  controllers.BankController.menu

+ nocsrf
POST    /bank/deposit               controllers.BankController.deposit

+ nocsrf
POST    /bank/withdraw               controllers.BankController.withdraw

+ nocsrf
POST    /bank/confirm/deposit       controllers.BankController.confirmDeposit

+ nocsrf
POST    /bank/confirm/withdraw       controllers.BankController.confirmWithdraw

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
